## inbox

F07 provides split-view `/inbox`, form settings at `/settings/forms`, public forms at
`/forms/:publicId` and hosted webchat at `/chat/:publicId`. The browser only receives an opaque
public form id; workspace APIs continue to use authentication cookies.
