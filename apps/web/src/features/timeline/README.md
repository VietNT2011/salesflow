## timeline

F04 renders the unified timeline and customer task panel inside Customer 360. Agents can log notes,
calls, emails and meetings, filter activity types, create follow-up tasks and complete assigned work.
`/tasks` is the Agent dashboard for today's, overdue or all open work. PII masking and authorization
remain server policies; hidden UI controls are not security boundaries.
